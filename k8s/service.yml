apiVersion: v1
kind: Service
metadata:
  name: flask-service
  labels:
    app: flask
    release: monitoring
spec:
  selector:
    app: flask
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP